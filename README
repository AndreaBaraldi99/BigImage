To launch the dashboard, just launch the ./launch_dashboard.sh command having the best_model_amp.pth and the app.py files in the folder

NOTE: the dashboard was tested on Linux, the compatibility with windows is not known